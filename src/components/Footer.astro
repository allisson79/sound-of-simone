---
---

<footer class="footer">
  <div class="footer-container">
    <div class="footer-content">
      <div class="footer-section">
        <h3>Sound of Simone</h3>
        <p>Fysioterapeut med fordypning i kvinnehelse</p>
      </div>
      <div class="footer-section">
        <h3>Kontakt</h3>
        <p>
          <a href="mailto:Hei@soundofsimone.no">Hei@soundofsimone.no</a><br />
          Tlf: <a href="tel:91170100">911 70 100</a>
        </p>
      </div>
    </div>
    <div class="footer-bottom">
      <p>&copy; {new Date().getFullYear()} Sound of Simone. All rights reserved.</p>
    </div>
  </div>
</footer>

<style>
  .footer {
    background-color: var(--color-burgundy);
    color: var(--color-white);
    padding: var(--space-16) 0 var(--space-8);
    margin-top: var(--space-24);
  }

  .footer-container {
    max-width: var(--max-width-7xl);
    margin: 0 auto;
    padding: 0 var(--space-6);
  }

  .footer-content {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: var(--space-12);
    margin-bottom: var(--space-12);
  }

  .footer-section h3 {
    font-size: var(--font-size-xl);
    margin-bottom: var(--space-4);
    font-weight: var(--font-weight-bold);
  }

  .footer-section p {
    line-height: var(--line-height-relaxed);
  }

  .footer-section a {
    color: var(--color-white);
    text-decoration: none;
    transition: opacity var(--transition-base) var(--ease-out);
  }

  .footer-section a:hover {
    opacity: 0.8;
  }

  .footer-bottom {
    border-top: 1px solid rgba(255, 255, 255, 0.2);
    padding-top: var(--space-8);
    text-align: center;
  }

  .footer-bottom p {
    font-size: var(--font-size-sm);
    opacity: 0.8;
  }
</style>

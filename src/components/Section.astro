---
const { title, subtitle, id } = Astro.props;
---

<section class="section" id={id}>
  <div class="container">
    {title && <h2 data-reveal>{title}</h2>}
    {subtitle && <p class="section-sub" data-reveal>{subtitle}</p>}
    <slot />
  </div>
</section>
